path_classifiers:
  test:
    - "../chassis/*.h"
    - "../chassis/*.cpp"
queries:
  - exclude: vcpkg/            
extraction:
  cpp:
    prepare:
      - bash ./setup.sh
      - mkdir build
      - cd build
    index:    # Customizable step used by all languages.
      build_command:
        - cmake ..