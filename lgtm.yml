path_classifiers:
  test:
    - "chassis/*.h"
    - "chassis/*.cpp"
queries:
  - exclude: vcpkg            
extraction:
  cpp:
    prepare:
      - build-essential
    after_prepare:
      - bash ./setup.sh
    before_index:
      - mkdir build
      - cd build
      - cmake ..