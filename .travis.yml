language: cpp 
compiler:
  - g++

os: linux 
dist: xenial 
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-7

script:
  - ls
  - bash vcpkg/vcpkg list
  - bash vcpkg/bootstrap-vcpkg.sh
  - make --version
  - mkdir build 
  - cd build
  - cmake ..