language: cpp 
compiler:
  - g++

os: linux 
dist: xenial 
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - build-essential 

script:
  - ls
  - make --version
  - mkdir build 
  - cd build
  - cmake ..